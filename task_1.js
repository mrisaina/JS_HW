const name = "Protective field generator";
let price = 1000;

console.log(`${name} selected, price per one ${price} credits`);

price = 2000;

console.log(`${name} selected, price per one ${price} credits`);
